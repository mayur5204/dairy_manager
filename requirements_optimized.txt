# Essential packages for dairy manager production
Django==5.2
django-crispy-forms==2.3
crispy-bootstrap4==2024.10
django-bootstrap4==25.1

# Production server
gunicorn==23.0.0
whitenoise==6.9.0

# Database (MySQL for PythonAnywhere)
mysqlclient==2.2.0

# PDF generation
reportlab==4.1.0
PyPDF2==3.0.1

# Excel export
xlwt==1.3.0

# Environment management
python-dotenv==1.0.1

# Required dependencies (auto-installed with Django but listed for clarity)
asgiref==3.8.1
sqlparse==0.5.3
tzdata==2025.2

# Note: Removed packages not used in production:
# - beautifulsoup4 (not used in the app)
# - soupsieve (dependency of beautifulsoup4)
# - dj-database-url (not used, using direct DB config)
# - psycopg2-binary (PostgreSQL, not needed for MySQL)
# - packaging (auto-dependency)
# - typing_extensions (auto-dependency for older Python versions)
